# 安全政策

## 支持版本

当前只维护最新发布版本。

## 报告安全问题

不要在公开 Issue 中披露以下内容：

- 能绕过匿名审计或泄漏盲测分组的问题；
- 能把本地绝对路径、作者身份、队号或文档元数据写入公开报告的问题；
- 任意文件覆盖、路径穿越、ZIP 路径逃逸或命令注入问题；
- 测试夹具或发布包中意外包含的论文、数据、凭据或私有映射。

优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交。仓库管理员应在公开仓库创建后启用 Private vulnerability reporting。

如果该按钮不可见，只能新建一个不含漏洞细节的普通 Issue，询问维护者可用的私密联系方式；不要在 Issue 中披露复现步骤、受影响文件或真实材料。

私密报告请提供：

1. 受影响版本和文件；
2. 最小复现步骤；
3. 实际影响；
4. 建议修复方向；
5. 在安全修复发布前是否已向其他人披露。

维护者确认问题前，请不要上传真实敏感论文作为复现材料。应使用最小合成文件。
